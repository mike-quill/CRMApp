@model List<BillingTerm>
@{
	ViewData["Title"] = "Sort";
}

@using (Html.BeginForm("Sort", "BillingTerms", FormMethod.Post))
{
	<h3>Drag and Drop to Reorder List</h3>
	<table id="BillingTerm" class="sortable table w-auto table-bordered">
		<thead class="thead-light">
			<tr>
				<th>ID</th>
				<th>Name</th>
			</tr>
		</thead>
		<tbody>
			@foreach (BillingTerm billingTerm in Model)
			{
				<tr>
					<td>
						@billingTerm.ID
						<input type="hidden" name="termId" value="@billingTerm.ID" />
					</td>
					<td>@billingTerm.Name</td>
				</tr>
			}
		</tbody>

	</table>
	<br />
	<input type="submit" value="Update Preference" class="btn ml-0"/>
	<div>
		<a asp-action="Index" asp-controller="Lookups" asp-route-Tab="BillingTermsTab">Back to Lookup List</a>
	</div>
	<script type="text/javascript">
		$(".sortable tbody").sortable();
		$(".sortable tbody").disableSelection();
	</script>
}







