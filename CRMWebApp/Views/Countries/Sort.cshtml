@model List<Country>
@{
	ViewData["Title"] = "Sort";
}

@using (Html.BeginForm("Sort", "Countries", FormMethod.Post))
{
	<h3>Drag and Drop to Reorder List</h3>
	<table id="Country" class="sortable table w-auto table-bordered">
		<thead class="thead-light">
			<tr>
				<th>ID</th>
				<th>Name</th>
			</tr>
		</thead>
		<tbody>
			@foreach (Country country in Model)
			{
				<tr>
					<td>
						@country.ID
						<input type="hidden" name="countryId" value="@country.ID" />
					</td>
					<td>@country.Name</td>
				</tr>
			}
		</tbody>

	</table>
	<br />
	<input type="submit" value="Update Preference" class="btn ml-0"/>
	<div>
		<a asp-action="Index" asp-controller="Lookups" asp-route-Tab="CountriesTab">Back to Lookup List</a>
	</div>
	<script type="text/javascript">
		$(".sortable tbody").sortable();
		$(".sortable tbody").disableSelection();
	</script>
}







