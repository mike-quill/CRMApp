@model List<Currency>
@{
	ViewData["Title"] = "Sort";
}

@using (Html.BeginForm("Sort", "Currencies", FormMethod.Post))
{
	<h3>Drag and Drop to Reorder List</h3>
	<table id="Currency" class="sortable table w-auto table-bordered">
		<thead class="thead-light">
			<tr>
				<th>ID</th>
				<th>Name</th>
			</tr>
		</thead>
		<tbody>
			@foreach (Currency currency in Model)
			{
				<tr>
					<td>
						@currency.ID
						<input type="hidden" name="currencyId" value="@currency.ID" />
					</td>
					<td>@currency.Name</td>
				</tr>
			}
		</tbody>

	</table>
	<br />
	<input type="submit" value="Update Preference" class="btn ml-0" />
	<div>
		<a asp-action="Index" asp-controller="Lookups" asp-route-Tab="CurrenciesTab">Back to Lookup List</a>
	</div>
	<script type="text/javascript">
		$(".sortable tbody").sortable();
		$(".sortable tbody").disableSelection();
	</script>
}







